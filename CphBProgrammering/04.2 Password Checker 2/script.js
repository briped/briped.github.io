//DONE omskriv passwordcheckeren fra uge 2 så den skriver til DOM'en i stedet for en alert
// Det var allerede det jeg gjorde. Jeg havde bare droppet script.js.

function doSubmit() {
    document.getElementById('output').innerHTML = document.getElementById('password').value.length;
}
